---
const { quote = "" } = Astro.props
const text = String(quote)
  .replace(/^[«“"‘']+|[»”"’']+$/g, "")
  .trim()
---
{text && (
  <figure class="pull-quote">
    <blockquote>
      <p>“{text}”</p>
    </blockquote>
  </figure>
)}
